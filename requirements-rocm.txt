# Athens HDL MCP - AMD GPU (ROCm 6.4) Dependencies
#
# For AMD GPUs with ROCm 6.4+ support (RX 9070 XT, RX 7000 series, etc.)
#
# Installation:
#   Recommended: bash scripts/setup_pytorch.sh (auto-detects hardware)
#   Manual: uv pip install -r requirements-rocm.txt
#
# Prerequisites:
#   - ROCm 6.4+ installed on system
#   - Verify with: rocm-smi
#
# Performance:
#   - ~15x faster embedding generation vs CPU
#   - bfloat16 precision on RDNA 3+ GPUs
#   - Auto-adjusts batch sizes for GPU VRAM

# PyTorch 2.9.0 with ROCm 6.4 support
--index-url https://download.pytorch.org/whl/rocm6.4
torch>=2.9.0
torchvision
torchaudio

# Core dependencies (same as requirements.txt)
docling>=2.54.0
sentence-transformers>=2.2.0
transformers>=4.35.0
pypdf>=3.0.0
